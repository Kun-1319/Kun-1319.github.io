---
layout: post
title: 'Bugku Misc telnet Writeup'
date: "2026-09-26 20:00:00 +0800"
categories: [CTF, Bugku, Misc]
---

## 一、 题目信息
- **平台**：Bugku
- **题型**：Misc（杂项）
- **考点**：Wireshark 流量分析、Telnet 明文协议抓包、TCP 流追踪

## 二、 解题思路与踩坑记录
刚拿到这道题，看到题目名 `telnet`，经过搜索得知是个网络流量包。下载附件解压后，得到了一个 `.pcap` 文件。
一开始我图省事，直接用 Windows 记事本打开了这个文件，在里面翻找，确实搜到了一串flag格式的字符，提交后却显示错误。

我决定安装专业的抓包分析工具 Wireshark，利用抓包功能在追踪流中找到了密码，也就是flag。

## 三、 解题步骤（核心解密过程）

1. **下载与解压**：下载附件，解压得到 `.pcap` 流量包文件。
2. **打开文件**：使用 Wireshark 软件打开该 `.pcap` 文件。
3. **过滤流量**：在顶部的“显示过滤器”栏中输入 `telnet` 并回车，过滤出所有 Telnet 协议的数据包。
4. **追踪 TCP 流**：
   - 在下方的数据包列表中，**右键点击**任意一个 Telnet 数据包。
   - 在弹出的菜单中选择 **`追踪流 (Follow)`** 的 **`TCP 流 (TCP Stream)`**。
5. **获取 Flag**：在password下面发现flag 。
6. **提交答案**：将 flag 复制并提交至 Bugku 平台
